{
  "name": "admin-prisma",
  "version": "1.0.0",
  "description": "Prisma setup for admin DB schema",
  "main": "seed.js",
  "scripts": {
    "dev": "node -v",
    "seed": "node prisma/seed.js",
    "generate": "npx prisma generate",
    "migrate": "npx prisma migrate dev --name init"
  },
  "dependencies": {
    "@prisma/client": "^4.17.0",
    "bcryptjs": "^2.4.3",
    "pg": "^1.21.0",
    "typescript": "^5.0.4"
  },
  "devDependencies": {
    "prisma": "^4.17.0"
  }
}
